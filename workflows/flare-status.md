---
description: Show all active worktrees with pipeline progress
---

When the user says "flare status", "status", or "what's running", execute:

// turbo-all

1. Run status: `flare status`
2. Report the status back to the user.
